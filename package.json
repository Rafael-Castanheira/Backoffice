{
  "name": "backoffice",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "start": "concurrently -n backend,frontend -c green,cyan \"npm --prefix backend start\" \"npm --prefix frontend run dev\""
  },
  "dependencies": {
    "pg": "^8.16.3",
    "pg-hstore": "^2.3.4",
    "sequelize-auto": "^0.8.8"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
